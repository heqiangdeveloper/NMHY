<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <include layout="@layout/activity_user_title"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">
        <TextView
            android:id="@+id/my_info_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dip"
            android:background="@color/white"
            android:drawableRight="@mipmap/more_icon"
            android:drawableLeft="@mipmap/settings"
            android:drawablePadding="@dimen/user_fragment_drawable_padding"
            android:padding="15dip"
            android:text="个人资料"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_16" />

        <TextView
            android:id="@+id/my_company_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dip"
            android:background="@color/white"
            android:drawableRight="@mipmap/more_icon"
            android:drawableLeft="@mipmap/settings"
            android:drawablePadding="@dimen/user_fragment_drawable_padding"
            android:padding="15dip"
            android:text="我的公司"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_16" />

        <TextView
            android:id="@+id/settings_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dip"
            android:background="@color/white"
            android:drawableRight="@mipmap/more_icon"
            android:drawableLeft="@mipmap/settings"
            android:drawablePadding="@dimen/user_fragment_drawable_padding"
            android:padding="15dip"
            android:text="设置"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_16" />
    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>
