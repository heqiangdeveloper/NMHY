<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/message_item_devid_color"
    android:fitsSystemWindows="true">
    <include layout="@layout/activity_edit_value_title"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:background="#f2f2f2"
        android:scrollbars="none"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">
        <EditText
            android:id="@+id/string_content_et"
            android:layout_marginTop="5dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:padding="8dip"
            android:text="sdadsada"
            android:hint=""
            android:drawableRight="@mipmap/white_icon"
            android:drawablePadding="@dimen/drawable_padding_size"
            android:textColorHint="@color/grey"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_16"/>
    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>
